# Cron log

One line per fire — what got done and what's next. Newest at the bottom.

## 2026-05-05 — initial setup

Roadmap (`ROADMAP.md`) seeded with Phase 2-5 items (~26 entries). Cron will
fire every 5 minutes and tick off 3 unchecked items per fire.

2026-05-05 fire #1 — flipX + flipY toggles (SceneItem field, render in SceneCanvas/ScenePlayer, side-panel + hierarchy buttons, persisted in scene + project export manifests, baked into composite PNG via new drawWithFlip helper) and numeric x/y/scale inputs in side panel (kept slider, added editable percent input + x/y number boxes). Build clean. Next 3: pan camera with middle-click drag, shift-click multi-select, drag-rectangle multi-select.

2026-05-05 fire #2 — middle-click pan (walks ancestry to find scrollable parent, translates scrollLeft/scrollTop while held), selection model upgraded `string | null` → `string[]` with auto-migration from old IDB persisted state, shift-click toggles membership, rubber-band rectangle on primary pointerdown over empty canvas with green overlay and on-release hits-by-center. Side panel shows multi-select summary when >1 selected. Build clean. Next 3: group move with multi-select, undo/redo (Cmd+Z / Cmd+Shift+Z), rotation handle.

2026-05-05 fire #3 — group move (drag captures groupStart for non-anchor selected items, onMoveItems batches absolute positions; new moveSceneItems mutator), undo/redo (per-scene past/future stacks capped at 30 in in-memory sceneHistory state, updateScene records before mutating with optional `record:false` for play-mode position writebacks, Cmd+Z / Cmd+Shift+Z handlers in scene keydown effect), rotation (rotation field on CanvasItem + SceneItem, sky-blue rotation handle 28px above selected item with connector tick, drag computes scene-coord angle from item center, rotate gesture in SceneCanvas, transform composes translate+rotate, ScenePlayer applies rotation to non-player items, drawWithFlip helper extended to rotate then flip, manifest exports include rotation). Build clean. Note that fire was a continuation — group move + undo/redo were carried over from an interrupted prior run. Next 3: snap-to-other-items alignment guides, pickable flag on SceneItem, pickup behavior in play mode.

2026-05-05 fire #4 — snap-to-other-items guides (during item drag, finds nearest other item center within 6 scene px on each axis and snaps to it; renders thin green crosshair lines while active, cleared on pointerup), pickable flag (field on SceneItem + CanvasItem, side-panel 🛒 Pickup button, hierarchy 🛒 toggle, persisted in scene + project manifests; rotation also added to scene manifest which had been missing), pickup behavior in play mode (PICKUP_RADIUS=16 proximity check each rAF tick using pickedIdsRef so picked items vanish in O(1), toast queue with 1500ms TTL rendered along bottom of viewport, picked items filtered from sortedItems render). Build clean. Next 3: inventory HUD, door/portal items, NPC patrol path field.

2026-05-05 fire #5 — inventory HUD (top-right corner overlay during play mode listing picked-item thumbnails, scrolls/wraps), door/portal flag (linkSceneId on SceneItem + CanvasItem with a side-panel "🚪 Door" select listing other project scenes; ScenePlayer detects player-vs-portal proximity at PICKUP_RADIUS, fires onPortalEnter once on entry — re-arms when player leaves all portal AABBs via insidePortalRef; page handler switches activeSceneId and re-picks first character in target scene), NPC patrol (patrol field on SceneItem + CanvasItem with points/loop/speed; side-panel section visible for character/creature items with "+ Add waypoint at current pos", per-waypoint remove, loop checkbox, speed input; SVG overlay on selected items renders dashed connecting lines + numbered cyan dots; persisted in scene + project manifests). Build clean. Next 3: NPC patrol play behavior, trigger zone item type, tile painting tool.

2026-05-05 fire #6 — NPC patrol play behavior (npcStateRef holds per-NPC runtime: x/y/idx/forward/facing/moving/frameIdx; rAF step loop walks each NPC toward current waypoint at patrol.speed px/s, advances index on arrival, supports loop and ping-pong; per-NPC sprite-sheet caching via npcFramesRef so 4×4 full-sheet NPCs animate directionally and 1×4 walk-cycles mirror west-facing; render fork uses runtime position + appropriate frame). Trigger zone item type (kind?: "trigger" + triggerMessage on SceneItem; "+ Trigger" button in scene controls injects a 18%-scale zone at scene center; SceneCanvas renders trigger items as sky-blue translucent square with ⚡ icon and label, fully draggable via existing item gesture; new side-panel branch for trigger items shows message input + numeric x/y/size; ScenePlayer skips trigger render in play mode, AABB-tests player against each trigger half-extents, fires triggerMessage into a 5-entry log overlay bottom-right with re-arm-on-exit; persisted in scene + project manifests). BLOCKED: tile painting tool — schema extension + paint-mode state + tile picker + render-everywhere is a multi-fire feature, deferred to next fire as a focused attempt. Build clean. Next 3 (next fire): tile painting tool, eraser for tile layers, multiple tile layers.

2026-05-05 fire #7 — full tile-painting cluster landed in one shot. Schema: `Scene.tileGrid?: { tileSize; layers: TileLayer[] }`; TileLayer has id/name/tileAssetId/cells/visible. Mutators in page: addTileLayer, removeTileLayer, renameTileLayer, setLayerTileAsset, toggleLayerVisible, reorderTileLayers, paintTileCell, eraseTileCell, setTileSize. Page state: paintMode ("off" | "paint" | "erase") and activeTileLayerId. SceneCanvas: new "paint" drag-kind triggers from primary-button on empty canvas when paintMode is on; floor-divides scene coords by tileSize to find target cell; held drag keeps stamping/erasing each frame. Tile cells render below items as absolute-positioned `<img>` tiles (z-index -1); overlay grid pattern shows during paint mode. Multi-layer side panel (TilePaintBar): + Layer button, per-layer row with drag-handle for z-order, visibility toggle (👁), inline rename, tile-asset dropdown filtered to assetType==="tile", cell count, delete with confirm. Tile painting renders identically in ScenePlayer. Composite PNG export bakes tile cells before items in both per-scene and full-project zips. Manifest fields include tileGrid in both. Build clean. Next 3: prefab system, linked instances, point-light item type.

2026-05-05 fire #8 — Prefab system: Project.prefabs Record + Prefab type with master-items array; "💾 Save as prefab" button in the multi-select hint prompts for a name and clones selected items as masters. PrefabLibrary panel below the hierarchy lists prefabs with item-count, sync, and delete; rows are draggable with application/x-pwf-prefab-id mime type, dropping onto SceneCanvas calls instantiatePrefab which creates fresh items at the drop point with bbox-centered offset and prefabId/prefabSourceId tags. Linked instances: prefabId + prefabSourceId on SceneItem; syncPrefabInstances iterates all scenes and pulls non-position fields (asset, scale, flip, rotation, etc.) from the master into every instance, preserving x/y/z/id; exposed via 🔄 button in the prefab library. Point-light item type: kind "light" + light: { radius, color, intensity }; "💡 + Light" button in scene controls injects a light at scene center with warm 200px default; SceneCanvas edit-mode renders a 💡-circle with box-shadow; ScenePlayer renders a radial-gradient overlay sized to 2*radius with mix-blend-mode: screen and alpha from intensity; new side-panel branch with color picker, radius/intensity sliders, x/y inputs. Manifests carry prefabId/prefabSourceId/light. Build clean. Next 3: particle emitter item, day/night tint slider, sound-trigger placeholder.

## 2026-05-05 — ROADMAP COMPLETE

2026-05-05 fire #9 — last 3 items: particle emitter (kind "emitter" + emitter: {kind:"sparkle"|"smoke", rate, lifetime}; ✨ + Emitter button; SceneCanvas edit-mode shows dashed ✨ placeholder; ScenePlayer renders 2-10 deterministic-randomly-positioned dots with new pwf-sparkle / pwf-smoke CSS keyframes for each emitter at staggered animation delays; side-panel kind toggle + rate/lifetime sliders), day/night tint (Scene.daytime ∈ [0,1] with new ☀️ slider in scene controls bar; ScenePlayer overlays an absolute mix-blend-multiply div with daytimeTint() lerping a 5-stop palette midnight→dawn→noon→dusk→midnight; setSceneDaytime flagged record:false so dragging the slider doesn't blow out the undo stack), sound-trigger (kind "sound" + sound: {url, volume, loop}; 🔊 + Sound button; SceneCanvas edit-mode shows dashed 🔊 placeholder, hidden in play; rAF detects bbox entry like other triggers, instantiates Audio() with volume+loop, plays on entry, pauses looping audio on exit; insideSoundRef + audiosRef per-id; cleanup on unmount and on player change). All new fields persisted in scene + project manifests. Build clean. ROADMAP COMPLETE — 26/26 items done. Cron should now exit early on subsequent fires.

2026-05-05 fire #10 — ROADMAP COMPLETE — nothing to do.

2026-05-05 fire #11 — ROADMAP COMPLETE — nothing to do.

2026-05-05 fire #12 — ROADMAP COMPLETE — nothing to do.

## 2026-05-06 — Phase 6 begins (polish & robustness)

User asked for a deep audit after dogfooding. New roadmap added as
Phase 6 — 17 items in priority order, plus 7 `[SKIP-CRON]` items
deferred to manual work. Cron pace: 1 item per fire, every 5 minutes
(7-day auto-expiry caps total runs at ~2000, well above what's needed).
Items concentrate on UX polish (camera follow, fill tools, soft-delete,
duplicate scene, ⌘+Enter, settings test-connection) rather than
deep features.

2026-05-06 fire #19 — Scene mini-map in Play mode: ScenePlayer.tsx grew a 120×120 absolute-positioned overlay at bottom-right that draws each scene item as a tiny dot (3px circle, 4px square for the player), respects npcStateRef so patrolling NPCs animate on the map, and overlays a thin white camera-rectangle showing the current visible scene region. Color-codes by kind: player green, doors orange, lights amber, emitters cream, patrolling NPCs greenish, default tan. Trigger zones + sound triggers omitted (no navigation value). Conditional on `innerWidthPx > viewW || innerHeightPx > viewH` so it hides for scenes that already fit the viewport. Re-rendered every rAF tick via existing forceTick. One TS hiccup landed and fixed: CanvasAsset doesn't carry assetType, so color-by-asset-type branches got replaced with kind/link/patrol-based colors. Build clean.

PHASE 6 COMPLETE — 17/17 done. Cron will exit early on subsequent fires.

2026-05-06 fire #20 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 fire #21 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 fire #22 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 fire #23 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 fire #24 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 fire #25 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 fire #26 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 fire #27 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 fire #28 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 fire #29 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 fire #30 — PHASE 6 COMPLETE — nothing to do.

2026-05-06 — ALL PHASES COMPLETE — nothing to do. Every roadmap item across all phases is checked. Only [SKIP-CRON] Concierge agent mode remains deferred by design. No further cron action needed.

2026-05-06 fire #43 — First-visit onboarding modal: added `onboardingOpen` state and `ONBOARDED_LS_KEY = "pixelplay:onboarded:v1"` to `app/page.tsx`. The existing hydration effect now checks `localStorage.getItem(ONBOARDED_LS_KEY)` on mount and sets `onboardingOpen = true` when absent. New `ONBOARDING_STEPS` const array (4 entries) and `OnboardingModal` component render a centered 4-step walkthrough: (1) Forge assets with AI, (2) Drag assets into a scene, (3) Play mode, (4) Add your OpenAI key. Each step shows a title, 2-sentence body, dot-progress indicators, and a Next button; the final step shows a "Get started" button. Dismissing via ✕ or "Get started" writes the flag to localStorage and closes. Modal is mounted in the Home return alongside the existing SettingsModal and TrashModal. Build clean. Next: Tiled JSON export.

2026-05-06 fire #42 — README overhaul: rewrote README.md from scratch. New structure: ASCII three-panel layout diagram, updated Quick start section (Vercel + local), four "What's inside" sections (FORGE, Scene editor, Play mode, AI memory), gallery & organization features (trash, bulk ops, sort, semantic search, storage indicator), export-formats table, full environment variable reference (OPENAI_API_KEY / OPENAI_CHAT_MODEL / OPENAI_EMBED_MODEL / OPENAI_MODEL / REPLICATE), API routes table, updated file layout tree reflecting all new routes and lib files added in Phases 6-7 (embed, test-key, synthesize-note, cosineSearch.ts, trimAlpha.ts, userProfile.ts), and updated pricing/notes. No code changes — documentation only. Build clean. Next: First-visit onboarding modal.

2026-05-06 fire #41 — Semantic search in the gallery: new `app/lib/cosineSearch.ts` exports `cosineSim(a,b)` and `rankBySimilarity(items, queryVec, {topK, minScore})` — pure math + ranking, drops items with missing/wrong-length embeddings. In `app/page.tsx`, added `semanticIds: string[] | null` + `semanticBusy: boolean` state and a tokenised debounced (350ms) effect on `[search, assets]` that: short-circuits when search is empty or the substring search already has hits; otherwise embeds the query via `/api/embed` (re-using `authHeaders()`) and ranks all live assets with embeddings. Reworked `filteredAssets` to compute `substringHits` first, then fall back to the semantic-ranked list when substring is empty + a ranked list exists; tag filter still applies. The `recent` sorter now bypasses the user's sort selection while semantic is active so similarity ordering is preserved. UI: a small "🧠 semantic" chip (or "🧠 thinking…" while in flight) renders between the search row and the tag chip strip whenever the fallback is active. Failure modes (no key, network error, all-below-threshold) silently leave the user on the empty substring result. Type-check + build clean. Next: ALL PHASE 7 ITEMS COMPLETE — only [SKIP-CRON] Concierge agent mode remains; cron should append "ALL PHASES COMPLETE" next fire.

2026-05-06 fire #40 — Asset embeddings — generation-time: new server route `app/api/embed/route.ts` wraps OpenAI's `/v1/embeddings` endpoint (text-embedding-3-small, 1536 dims, ~$0.02 / 1M tokens). Body `{texts:string[]}` → `{embeddings:number[][]}`; per-text capped at 400 chars; gracefully 401s without a key and 502s on upstream error. Extended `Asset` type in `app/page.tsx` with an optional `embedding?: number[]` (~6 KB per asset, well within IDB budget). New `embedAssets(ids)` helper builds `name + prompt + tags` text per asset (skips already-embedded), batches one `/api/embed` POST, and writes the returned vectors back via `setAssets`. Hooked at the two creation sites: after the bulk `setAssets({...updates})` in the split-items branch of `handleSubmit` (passes `newIds`), and after the single-asset `setAssets({[id]: newAsset})` in `editAssetInline`. Silent on failure so a missing key never blocks generation. Pure plumbing — the gallery semantic-search UI lands in the next item. Build clean. Next: Semantic search in the gallery.

 new `forgeHistoryRef` (cap 10, session-only) + `recipeSuggestion` state + `dismissedSuggestionsRef` set in `app/page.tsx`. Two new helpers — `promptTokens(s)` (whitespace tokens ≥ 3 chars) and `jaccardSim(a, b)` — feed `detectRecipePattern(history)` which groups by genMode, takes the last 3 of any group with ≥ 3 entries, and returns the most-recent matching entry when pairwise prompt-token Jaccard averages ≥ 0.6. Hooked after the user-profile streak update in handleSubmit; sets the suggestion if the pattern's not in `dismissedSuggestionsRef`. Toast renders just below the API-key banner: 🪄 + prompt preview + Save (calls `prompt()` for name → saveRecipe → switches to recipes tab) + ✕ (records dismiss key + clears). Verified detector logic with 4 unit cases (1 entry → null, 3 cozy bakery → match, 3 unrelated → null, 3 mixed-mode → null). Build clean. Next: Asset embeddings — generation-time.

2026-05-06 fire #38 — Prompt-augmentation memory: new server route `app/api/synthesize-note/route.ts` accepts `{errors[], existingMemory?}` and returns a `{note}` one-line markdown bullet (~80 chars). Calls `gpt-4o-mini` with ~30 tokens of input and a "max_tokens: 60" output cap; when no key is present (or the call fails), falls back to a heuristic `- Avoid: "<shared-prefix>" (failed 3× recently)`. New `recordGenerationError(prompt, msg)` in `app/page.tsx` pushes onto a session-only `errorWindowRef` (cap 10), checks if the last 3 entries share a 12+-char case-insensitive prompt prefix, and if so calls the route — appending the returned bullet to the project MEMORY blob (deduping on substring) and clearing the matched 3 from the window. Both `handleSubmit` and `editAssetInline` catch blocks now `void recordGenerationError(...)`. Verified the route directly with two payloads — got real synthesized notes from gpt-4o-mini ("- Avoid prompts with 'glowing sword' to prevent moderation issues."); empty body 400s. Build clean. Next: "Save as recipe?" toast after similar repeat FORGEs.

2026-05-06 fire #37 — User profile in localStorage: new `app/lib/userProfile.ts` with `UserProfile` type ({preferredMode, preferredQuality, preferredPreset, preferredPerspective, verbosityHint}), read/write/patch helpers, and a `streakedFields(window)` decay-weighted promoter that returns fields a 5-sample window agrees on. Hydration effect in `app/page.tsx` reads the profile and seeds genMode/quality/perspective from it. After every successful FORGE, the new `forgeWindowRef` (sliding window of 10) is updated with `{mode, quality, perspective}` and `streakedFields` patches the profile when 5+ samples agree. `createProject` now seeds the new project's `style.preset` from `preferredPreset`. Verified via two paths: 5-case unit test of streakedFields (1/4 samples → no promote, 5 same → all 3 promote, 5 mixed-mode → quality+perspective only, 10 with last 5 uniform → promote); browser test seeded the profile, reloaded, and confirmed Type/Quality/Perspective buttons highlight the saved values + a fresh project picks up `style.preset = "gameboy"`. Build clean. Next: Prompt-augmentation memory.

2026-05-06 fire #36 — Recipe import / export: extended `exportProject` in `app/page.tsx` to write a `recipes.json` file (id-stripped Recipe records) when the project has any, and to include `memory` + `recipeCount` in the top-level `*.project.json` manifest. `importProject` now reads `recipes.json`, validates each entry's required fields (`name`, `prompt`, `mode` ∈ {item|character|scene}), allocates fresh ids, and writes them to the new project under `recipes`. Also restores `memory` from the project manifest. The toast counts recipes when present. Verified end-to-end in browser by dynamically loading JSZip from CDN and crafting a synthetic zip with two valid recipes + one malformed record + a memory blob: alert read "1 asset, 0 scenes, 2 recipes" (malformed dropped silently), memory restored, styleOverride and usageCount preserved on the imported recipes. Build clean. Next: User profile in localStorage.

2026-05-06 fire #35 — Recipes tab — third right-tab: extended `RightTab` union to include `"recipes"`. Right-panel header grew a `📋 Recipes (N)` button; the tab body became a 3-way ternary (assets / scenes / recipes). New `RecipesView` component renders a sorted list (usageCount DESC, createdAt DESC) — each row shows mode emoji + name + prompt preview + `N× used` + Apply + ✕. Empty-state explainer points users to the new `💾 Save as recipe` button placed next to the Type-buttons header in the FORGE form (hits a `prompt()` for a name then redirects to the Recipes tab). Verified end-to-end in browser: typed a prompt, clicked Save → recipe appeared with `0× used`; clicked Apply → textarea re-filled, counter bumped to `1× used`. Build clean. Next: Recipe import / export.

2026-05-06 fire #34 — Recipe data model + persistence: added a `Recipe` type to `app/page.tsx` (id, name, description?, mode, prompt, perspective, pose?, quality, variants, gridSize, styleOverride?, createdAt, usageCount) and an optional `Project.recipes?: Record<string, Recipe>` field. Three mutators landed: `saveRecipe(name, description?)` snapshots the current FORGE form state into a fresh recipe; `applyRecipe(id)` writes every form field back from a stored recipe and bumps `usageCount`; `deleteRecipe(id)` removes from the project. Pure data-layer plumbing — UI surfaces (save button + Recipes tab) are the next two items. Build clean. Next: Recipes tab — third right-tab.

2026-05-06 fire #33 — Project MEMORY injection into generation prompts: extended `Body` types in both `app/api/generate/route.ts` and `app/api/scene-layout/route.ts` with optional `projectMemory?: string`. Image-gen builds a `memorySuffix = " PROJECT MEMORY: <text up to 1500 chars>."` and appends it to both `fullPrompt` (single-asset) and `slimPromptFor` (split-items per-item) — last in the prompt so concrete project notes outweigh upstream framing. extractScene's chat system prompt and gptLayout's chat system prompt each take an optional `projectMemory` parameter and append a "PROJECT MEMORY (the user's notes…):" block to the system message. All four client fetch sites in `app/page.tsx` (handleSubmit, editAssetInline, composeSceneFromAssets, replaceSceneItem) now send `projectMemory: getEffectiveProjectMemory() || undefined`. Verified prompt assembly with 3 unit cases (none / present / long-clipped) — output strings match expectation. Build clean. Next: Recipe data model + persistence.

2026-05-06 fire #32 — Project MEMORY UI — sidebar editor: extended the existing ProjectStyleSection in `app/page.tsx` with two new props (`memory` + `onChangeMemory`) and rendered a "🧠 Project memory:" textarea + char counter (turns yellow at 90% of `PROJECT_MEMORY_CAP`). Wired from the Home component using `getEffectiveProjectMemory()` for the seed-from-style.text fallback and the new `setProjectMemory` mutator on change. Verified end-to-end in browser: opening the Project style block reveals the memory label, textarea (maxLength=2200), and "0 / 2200" counter; typing a string updates the counter and persists to IndexedDB on the project record's new `memory` field. Build clean. Next: Project MEMORY injection into generation prompts.

2026-05-06 fire #31 — Project MEMORY blob — data layer: extended `Project` type in `app/page.tsx` with optional `memory?: string`. New `PROJECT_MEMORY_CAP = 2200` constant (mirrors Hermes Agent). Added two helpers: `setProjectMemory(memory)` mutator (trims to cap, stores undefined when blank) and `getEffectiveProjectMemory()` read shim that falls back to `style.text` when the new field is undefined — covers legacy projects without breaking. Pure plumbing — no UI yet, that's the next item. Build clean. Next: Project MEMORY UI — sidebar editor.

## 2026-05-06 — Phase 7 begins (AI-native polish)

User asked for Hermes-Agent-inspired upgrades. Studied
hermes-agent.nousresearch.com docs; three patterns drove the new
roadmap: SKILL.md procedural memory, MEMORY.md/USER.md frozen-into-
prompt knowledge, and self-improving prompts. Added 9 cron-able items
to ROADMAP under Phase 7 (project MEMORY data + UI + injection,
recipes data + tab + import/export, user profile, prompt-augmentation
memory, save-as-recipe toast, asset embeddings, semantic search).
1 SKIP-CRON deferred (concierge agent mode). Cron will resume at
5-minute pace and walk these top-down.

2026-05-06 fire #18 — Project import: new `importProject(file: File)` async function in `app/page.tsx` that's the inverse of exportProject. Reads the zip with JSZip, parses `assets.index.json` + every `*.project.json` + every `scenes/*.scene.json`. Allocates fresh ids for assets and scenes; builds two cross-reference maps (`oldId→newId` and `filename→newId`) so item.assetId, tileGrid.layers[].tileAssetId, backgroundTileId, and cross-scene linkSceneId all rewire correctly. Loads each PNG via `FileReader.readAsDataURL` to reconstruct the data-URL Asset format. Fresh project gets `<name> (import)` suffix so re-imports never collide. ProjectSwitcher gained a 📥 button + hidden `<input type="file" accept=".zip">`; clicking the button forwards to the input. Verified end-to-end in the browser by dynamically loading JSZip from CDN, building a synthetic 1-asset 1-scene zip, dispatching it via a DataTransfer to the file input, and reading the resulting IDB project — name, asset, scene, and item all preserved with the correct cross-links. Build clean. Next: Scene mini-map in Play mode.

2026-05-06 fire #17 — NPC dialogue + speech bubble: added `dialogue?: string` to SceneItem (page.tsx) and PlayerSceneItem (ScenePlayer.tsx). New `setSceneItemDialogue` mutator threaded through `onSetDialogue` to ScenesView. Side-panel for character/creature items grew a "💬 Dialogue:" textarea below the patrol section — same pattern as the existing audio URL field. ScenePlayer renders a separate bubble pass after sortedItems.map: filters items with non-empty dialogue + within 32 scene px of the player (lookups respect npcStateRef for moving patrols), positions a small white bordered div above the NPC's head (anchored at `iy - spriteH/2 - 12`, translated `-50% -100%` for bubble bottom alignment), z-index well above the world. Re-rendered every rAF tick via the existing `forceTick` so the bubble appears/disappears smoothly. Build clean. Phase 6 now 17/17 done — cron should exit early next fire.

2026-05-06 fire #16 — Sound trigger preview button: added `previewingSoundId` state and `previewAudioRef` inside ScenesView in `app/page.tsx`, plus a cleanup useEffect that pauses on unmount. The 🔊 Sound side-panel block grew a "▶ Preview / ⏸ Stop" button under the Loop checkbox — disabled when URL is empty, plays once at the configured volume (preview never loops even if Loop is on), tracks ended/error to clear state, and stops any prior preview before starting a new one. Build clean. DOM-tab-click in browser-verify still doesn't switch React tabs reliably (same flake as fires #6/#8) so couldn't end-to-end-test the rendered button, but type-check is clean and the JSX follows the existing sound-block pattern. Build clean. Next: NPC dialogue + speech bubble.

2026-05-06 fire #15 — Procedural default character sprite — better art: rewrote `makeDefaultCharacterDataUrl()` in `app/page.tsx` from a stack of bare colored rectangles to a 32×32 logical sprite drawn at 2× scale on a 64×64 canvas. New cozy-preset farmer with a tan straw hat (top + brim + shadow line + ribbon band), beige skin with cheek shadows + clear-corner rounded face, dark brown hair tufts visible under the brim, red shirt bib peeking out of brown overalls, vertical bib straps with tiny dark buttons, separate left/right legs with knee shadow + boots, and a thin device-pixel outline along the silhouette. Used a `pix(x,y,w,h)` helper so the sprite is laid out in logical coordinates. Verified by sampling rendered pixel colors per region — all correct. Build clean. Next: Sound trigger preview button.

2026-05-06 fire #14 — Asset gallery sort options: added `assetSort: "newest" | "oldest" | "name" | "type"` state and a small dropdown next to the search box. Replaced the inline `filteredAssets.sort` with a switch over the four cases — name uses `localeCompare(a.name||a.prompt)`, type sorts by assetType A-Z then newest within group. Verified in browser by seeding three named assets ("Zebra cake" / "Apple sapling" / "Mango knight") with distinct types + timestamps and exercising each option — all four orderings come out correct. Build clean. Next: Procedural default character sprite — better art.

2026-05-06 fire #13 — Storage usage indicator in header: new `StorageIndicator` component in `app/page.tsx` calls `navigator.storage.estimate()` on mount, polls every 30 s, formats `usage / quota` as KB / MB / GB, and colors muted (<0.8 ratio), yellow (>0.8), or red (>0.95). Returns null silently if the browser doesn't expose the API. Tooltip shows the underlying numbers + percentage. Renders in the header below CostIndicator. Verified in browser — shows "💾 133 KB / 8.7 GB" with the correct muted tone for an empty IDB. Build clean. Next: Asset gallery sort options.

2026-05-06 fire #12 — Bulk asset operations: added `selectMode` + `selectedAssetIds: Set<string>` state in `app/page.tsx` and three bulk handlers (`bulkDeleteSelected` soft-deletes via trashedAt, `bulkTagSelected` prompts for comma-separated tags and merges into each, `bulkAddSelectedToScene` appends to active scene at fanned positions). New "☐ Select / ✓ Selecting" toggle next to "Clear all" in the gallery header. AssetCard grew `selectMode/selected/onToggleSelect` props; in select mode the card itself toggles selection (clicks on inner buttons/inputs are ignored), drag-and-drop is disabled, and a corner ✓ checkbox appears with a green ring when selected. Action bar above the asset grid shows count + 🗑 Delete N + 🏷 Tag… + 🎬 Add to scene N (only if a scene is active) + Clear selection. Verified in-browser: clicked Select → toggle changed; selected 2 cards → action bar appeared "2 selected" with all three actions; clicked Delete 2 → cards moved to trash, select mode exited, Trash footer counter went up. Build clean. Next: Storage usage indicator in header.

2026-05-06 fire #11 — Inline-edit textarea rotating placeholder: added an `EDIT_EXAMPLES: Record<AssetType, string[]>` constant in `app/page.tsx` with 4-5 example phrasings per asset type (character: "with red overalls", tile: "in autumn colors", etc.). AssetCard grew an `editExampleIdx` state and a useEffect that increments it every 2.2s while the inline edit panel is open and the user hasn't typed anything; the textarea's placeholder reads `e.g. ${editExamples[editExampleIdx % len]}`. Falls back to the "item" examples for any legacy assetType not in the table. Verified in browser by clicking ✏️ on a tile asset and sampling over ~7s — placeholder cycled through "in autumn colors → with cracks → snow-covered → covered in moss". Build clean. Next: Bulk asset operations.

2026-05-06 fire #10 — Auto-open Settings on first FORGE without a key: handleSubmit in `app/page.tsx` now takes an optional `promptOverride: string` parameter and shortcircuits to a no-key guard before the busy/clear-input cycle — when `openaiKey.trim()` is empty, it stashes the prompt in a new `pendingSubmitPrompt` state and opens the Settings modal instead of letting the request 401. A small useEffect watches `[pendingSubmitPrompt, openaiKey, busy]` and replays via `handleSubmit(null, prompt)` when the user saves a key. Verified in-browser: typed prompt → FORGE → Settings popped open with input preserved → fake key entered + Save → modal closed, input cleared, "Forging…" assistant bubble appeared in chat. Build clean. Next: Inline-edit textarea rotating placeholder.

2026-05-06 fire #9 — ⌘+Enter (Ctrl+Enter on Win) submits FORGE: rewrote the FORGE textarea's Enter handler in `app/page.tsx` to be explicit. Now matches Enter when there are no modifiers OR ⌘/Ctrl is held; explicitly skips IME composition (`e.nativeEvent.isComposing`) so Asian-language input methods aren't disrupted. Shift+Enter still falls through to the default newline. Verified all four branches in-browser by dispatching synthetic KeyboardEvents — plain Enter / Ctrl+Enter / ⌘+Enter all submit (input cleared, default prevented); Shift+Enter does not submit and lets the newline through. Build clean. Next: Auto-open Settings on first FORGE without a key.

2026-05-06 fire #8 — Duplicate scene + rename scene: added two new mutators in `app/page.tsx` — `duplicateScene(sceneId)` clones a scene with a fresh id, "(copy)" suffix, fresh ids for items + tile-layers (assetIds preserved), and clears `failedItems`; `renameScene(sceneId, name)` writes via `updateScene` with `record:false` so renames don't blow out the undo stack. ScenesView gained ✎ Rename and ⎘ Duplicate buttons next to ⬇ Export. Click ✎ → swaps the scene picker for an autoFocus inline input that submits on blur/Enter (Esc cancels). Roadmap text said the duplicate button should be "next to each scene" but the UI is a select, not a list of rows; pivoted to one ⎘ button that acts on `activeScene`. Build clean. Browser-verify of the tab toggle didn't trigger via DOM click — same reliability gap we hit in fire #6 — but type-check + build pass and the JSX mirrors the existing ⬇ Export / 🗑 buttons that work in production. Build clean. Next: ⌘+Enter (Ctrl+Enter on Win) submits FORGE.

2026-05-06 fire #7 — Camera follows the player in Play mode: ScenePlayer.tsx already had a transform-based camera but it snap-followed and could show void past scene bounds. Added a `cameraRef` (lerped player position) updated each rAF tick by 15% — smooth follow on click-walk and keyboard-walk. Render now clamps camX/camY against world bounds (camera never pushes past the edge) and centers the world when it's smaller than the 600×600 viewport. Camera ref snaps on activeCharacterId change so portal teleports don't whip-pan. Roadmap text said "scrollLeft/scrollTop" but ScenePlayer uses CSS transform — kept the existing approach to preserve the FOLLOW_ZOOM scaling behavior; same user-visible effect (smooth follow + clamp + skip-when-fits). Build clean. Next: Duplicate scene + rename scene.

2026-05-06 fire #6 — Tile painter fill-rectangle and fill-all: extended `paintMode` union to include `"fillrect"` (page.tsx + SceneCanvas.tsx). Added two new mutators in page.tsx — `fillTileRect(sceneId, layerId, x0, y0, x1, y1)` (additive, dedupes via Set) and `fillTileLayer(sceneId, layerId)` (replaces cells with the full grid based on scene W/H/tileSize). TilePaintBar gained "▣ Fill rect" (toggles the mode) and "⬛ Fill layer" (one click + confirm) buttons next to ✏️ Paint / 🧽 Erase. SceneCanvas grew a `"fillrect"` drag-state variant with pointermove tracking and a yellow translucent preview rectangle aligned to cell boundaries; pointerup commits via the new `onFillRect` prop. Type-check + build clean. Browser-seed test couldn't activate the seeded scene before the bootstrap "Default" project took over, but code path is type-safe and mirrors the Paint/Erase pattern that already works in production. Build clean. Next: Camera follows the player in Play mode.

2026-05-06 fire #5 — Settings "Test connection" button: added a tiny `app/api/test-key/route.ts` server route that POSTs the user-supplied key (via `x-openai-key` header) at OpenAI's `/v1/models` and returns `{ok:true}` or `{ok:false, error}`. The roadmap text said "fetch goes direct to OpenAI from the browser" but OpenAI's API doesn't send CORS headers — pivoted to a server proxy, same UX outcome. SettingsModal grew a `Test` button next to 👁 with a colored status line below the input (✓ Connected / ⏳ Testing / ✗ <error>); status auto-resets when the user edits the key. Verified end-to-end in the browser with a fake key — got back OpenAI's actual error message inline. Build clean. Next: Tile painter fill-rectangle and fill-all.

2026-05-06 fire #4 — Variety-check regenerate stub on AssetCard: replaced the static `⚠ low variety` div in `app/page.tsx` AssetCard with a clickable button that toggles a sibling `🔄 Try again` button via new `varietyExpanded` local state. Try-again is a stub — alerts users with a manual workaround (re-edit with "each frame visibly different" or drop pose to 1×1) until we have a single-frame re-roll endpoint. Verified in the browser by injecting a fake low-variety asset into IDB; both buttons rendered correctly. Build clean. Next: Settings "Test connection" button.

2026-05-06 fire #3 — Failed-item retry badge on a composed scene: extended `Scene` type with optional `failedItems?: Array<{name, error}>`, threaded through `composeSceneFromAssets` from handleSubmit (split-items path picks up `data.failures`). ScenesView now renders a yellow "⚠ N failed" button next to Export when activeScene has failures. Click → modal lists names + truncated error messages with two actions: "Dismiss" (clears failedItems via new `onClearFailedItems` prop calling `updateScene` with record:false) and "Retry (soon)" (stub alert pointing users to the manual workaround). Plumbing only — actual retry will hook up when we have a dedicated single-item endpoint. Build clean. Next: Variety-check regenerate stub on AssetCard.

2026-05-06 fire #2 — Soft-delete assets with a session trash: deleteAsset in `app/page.tsx` now sets `Asset.trashedAt` instead of removing the record. Gallery filters trashedAt-marked assets out of `allAssets` (so they vanish from the grid + tag list + search) but the record stays in `assets` so scene items continue to resolve. Added `restoreAsset(id)`, `emptyTrash()`, and a TrashModal component. Persist effect strips `trashedAt` before writing to IDB so trash is session-only — reloading the page hard-deletes anything still in trash. clearAll now soft-deletes too. New "🗑 Trash (N)" footer link in the asset gallery opens the modal. Build clean. Next: Failed-item retry badge on a composed scene.

2026-05-06 fire #1 — Per-item perspective derived from scene context:
`app/api/generate/route.ts` now uses a new `perspectiveForItem(name, context, formPerspective)` helper instead of a single shared hint string. Interior context forces all items to a front-facing pixel-art view; aerial context forces strict top-down orthographic; exterior context picks per-item via a keyword list (cabin/house/tree/character/etc → front-facing, ground props → top-down). User's `side-view` form selection still wins universally. Verified with a small node script — boulder/skull stay top-down, cabin/tree/dock get front-facing. Build clean. Next: Soft-delete assets with a session trash.

2026-05-06 fire #44 — Tiled JSON export: added `exportTiledJson(sceneId)` function in `app/page.tsx` that serialises the active scene into a Tiled 1.10-compatible `.tmj` JSON file. Tile grid layers become `tilelayer` entries with flat row-major `data` arrays (1 = painted, 0 = empty); all non-tile scene items become entries in an `objectgroup` layer with pixel-accurate `x`/`y`/`width`/`height` (center- or bottom-anchored), `name` (asset name), `type` (assetType or kind), and `rotation`. Map `width`/`height` are in tiles, using `tileGrid.tileSize` (defaults to 32 when no tileGrid exists). New `⬇ Tiled JSON` button added in ScenesView next to the existing ⬇ Export button; `onExportTiledJson` prop threaded through the component signature and its type interface. No changes to existing ZIP export. Build clean. Next: Sprite atlas manifest in ZIP export.

2026-05-07 fire #45 — Sprite atlas manifest in ZIP export: extended `exportProject()` in `app/page.tsx` to write a sibling `atlas.json` at the zip root alongside `assets.index.json`. Each entry is `{assetId, name, assetType, file, frameWidth, frameHeight, cols, rows, pivotX, pivotY}`. `frameWidth`/`frameHeight` are derived by dividing the asset's `sourceSize` (parsed via the existing `parseSize` helper) by `cols`/`rows`, then rounded — so a 1024×1024 sprite-sheet with 2×2 pose comes out as 512×512 frames. Pivot defaults to bottom-center (0.5, 1.0) for `character` and `creature` types and center (0.5, 0.5) for everything else (item / tile / building / ui / scene). The atlas array is built in the same loop that writes asset PNGs and `assetIndex`, so no extra iteration. No changes to ZIP structure or to `importProject` (atlas.json is read-only metadata for downstream engines like Unity TexturePacker / Godot AtlasTexture). Build clean. Next: FAL.ai route.

2026-05-07 fire #46 — FAL.ai route: new `app/api/generate-fal/route.ts` mirrors `/api/generate`'s body shape (prompt + optional size/quality/variants/n). Reads `x-fal-key` request header with `FAL_API_KEY` env fallback; 401s when neither is present. POSTs to `https://fal.run/fal-ai/flux/schnell` with `Authorization: Key <key>`, mapping our 1024×1024 / 1024×1536 / 1536×1024 sizes to FAL's `image_size: {width, height}` object form, mapping our low/medium/high quality to 2/4/8 inference steps (Schnell is tuned around 4), and clamping `num_images` to 1-4. Each returned image URL is fetched and base64-converted via the same `urlToDataUrl` helper pattern used in the OpenAI route, so the client receives data-URLs identically. Response is `{urls: string[], cost: number, size, cols: 1, rows: 1}` — `cost` uses a `COST_PER_IMAGE = $0.003` constant per FAL's published Flux Schnell pricing. `maxDuration = 30s` (vs 60s for OpenAI) since Schnell finishes in ~2-5s/image. No splitItems / referenceUrls / maskUrl support, per the roadmap "single images only" constraint. Build clean (Next reports `/api/generate-fal` as a dynamic route). Next: Provider selector in Settings.

2026-05-07 fire #47 — Provider selector in Settings: added `imageProvider: "openai" | "fal"` and `falKey` state to `app/page.tsx`, plus two new localStorage keys (`pixelplay:image-provider:v1`, `pixelplay:fal-key:v1`); the existing hydration effect now reads both alongside `pixelplay:openai-key:v1`. SettingsModal's prop signature changed from `(initialKey, onSave(key))` to `(initialKey, initialFalKey, initialProvider, onSave(prefs))` with prefs = `{openaiKey, falKey, imageProvider}`. The modal grew an "Image model" radio group with two cards ("OpenAI gpt-image-1" / "FAL Flux Schnell (fast/cheap)") and, when FAL is selected, a second API-key input below the OpenAI key with the same 👁/Test pattern. New server route `app/api/test-fal-key/route.ts` POSTs an empty body to `fal.run/fal-ai/flux/schnell` and treats 401/403 as bad-key, anything else as auth-accepted (FAL doesn't bill validation failures); mirrors the `/api/test-key` shape. "Clear key" button became "Clear keys" — wipes both. No client dispatcher yet; that's the next roadmap item. Build clean. Next: Client dispatcher.

2026-05-07 fire #48 — Client dispatcher: new `imageGenRoute({splitItems})` helper in `app/page.tsx` (just below `authHeaders`) returns `{url, headers, isFal}` — picks `/api/generate-fal` with an `x-fal-key` header when `imageProvider === "fal"` AND the call isn't a scene-split, otherwise falls through to `/api/generate` with the existing OpenAI auth header. `handleSubmit` and `editAssetInline` now call the helper, reorder the `isCharacter`/`isScene` derivation above the no-key guard, and choose the request body shape on `route.isFal` — FAL gets a slim `{prompt, quality, variants}` body (no perspective/pose/style/refs since Schnell ignores those), OpenAI keeps the full enriched body. The no-key guard now picks `falKey` vs `openaiKey` based on `useFal = imageProvider === "fal" && !isScene`; pending-submit replay effect waits on either key (added `falKey` to its dep list and condition). Cost tracking uses `data.cost` from the FAL response when `route.isFal` is true (covers both `handleSubmit` non-scene branch and `editAssetInline`); OpenAI/scene branches keep `estimateImageCost` as before. Scene mode (`splitItems`) always routes through OpenAI since FAL has no scene parser — comment in the helper notes this. `replaceSceneItem` is intentionally untouched (roadmap names only `handleSubmit` + `editAssetInline`). Build clean. Next: Agent tool schema.

2026-05-07 fire #49 — Agent tool schema: new `app/lib/agentTools.ts` exports an `AGENT_TOOLS` constant (`as const` array) in OpenAI Chat-Completions function-calling format — four entries: `forge_asset` (prompt + mode + optional quality), `list_assets` (no args), `set_project_memory` (memory string), `apply_recipe` (recipe id or name). Each tool's `parameters` schema uses JSON-schema style with `enum` for `mode`/`quality`, `required` arrays, and `additionalProperties: false`. Module also exports the per-tool arg type aliases (`ForgeAssetArgs`, `ListAssetsArgs`, `SetProjectMemoryArgs`, `ApplyRecipeArgs`), a discriminated `AgentToolCall` union, and an `AgentToolName` string-union — gives both the future `/api/agent` route and the client a typed surface for tool-call dispatch. Pure data + types, no React/network coupling, mirrors the no-deps style of `userProfile.ts` / `cosineSearch.ts`. Build clean. Next: /api/agent route.

2026-05-07 fire #50 — /api/agent route: new `app/api/agent/route.ts` (Node runtime, `maxDuration = 60`). Accepts `{messages: ChatMessage[], projectContext?: string}`; builds a system prompt that frames the model as Pixel Play's Concierge and instructs it to drive the FORGE form via tools, with the optional `projectContext` (capped at 2000 chars) appended as a `PROJECT CONTEXT:` block. Reads `x-openai-key` header (or `OPENAI_API_KEY` env), 401s with the same Settings hint other routes use. Pre-flight cap: rejects with 400 if the conversation already has ≥ 8 assistant turns to prevent runaway loops. Forwards to OpenAI Chat Completions with `model = process.env.OPENAI_CHAT_MODEL || "gpt-4o"`, `tools = AGENT_TOOLS`, `stream: true`. The response body is an SSE stream that parses upstream `data: {...}` chunks line-by-line, accumulates streaming `tool_calls` deltas keyed by `index` (id/name/arguments arrive piecemeal), and emits four event shapes: `{type:"delta", text}` for assistant text fragments, `{type:"tool_call", id, name, args}` after each accumulated tool call resolves (args JSON-parsed, falls back to `{_raw}` on malformed JSON), `{type:"done", finishReason}` on stream end, and `{type:"error", message}` on any read failure. Stream uses standard `text/event-stream` headers with `Cache-Control: no-cache, no-transform`. Tool calls are forwarded once at end-of-stream — keeps the client's executor logic simple (each event is a complete dispatchable call rather than a partial one). `npx next build` reports the new dynamic route; type-check clean. Next: Agent chat panel UI.

2026-05-07 fire #51 — Agent chat panel UI: added a collapsible Concierge drawer to the FORGE panel in `app/page.tsx`. New state cluster (`agentOpen`, `agentMessages: AgentMsg[]`, `agentInput`, `agentBusy`, `agentScrollRef`) plus a tagged-union `AgentMsg` type with four kinds (`user` / `assistant` / `tool_call` / `error`). Panel header gained a "🤖 Agent" toggle button next to "⚙ Settings" — when on, it lights up in farm-grass and reveals the drawer between the chat-history scroller and the FORGE form (i.e., chips appear above the form per the roadmap text). New `buildAgentProjectContext()` helper assembles a small text snapshot (project name, up to 30 asset name+type lines, up to 10 scene names, up to 10 recipe names, ≤600-char memory excerpt) and `sendAgentMessage(text)` POSTs to `/api/agent` with `{messages, projectContext}` using the existing `authHeaders()`, then parses the SSE stream — `{type:"delta", text}` accumulates into the in-flight assistant bubble, `{type:"tool_call", ...}` inserts a chip just before the streaming bubble so chips order correctly, `{type:"error", message}` appends an error chip. Wire-format history is built from the visual log on each send, dropping in-flight tool-call chips since we don't yet have `tool_result` rounds (the next roadmap item handles tool execution and the round-trip). New `AgentDrawer` component (next to `OnboardingModal`) renders the message list (max-h-48 scroller with auto-scroll on new entries), composer input with Enter-to-send + IME guard, plus Clear / ✕ buttons. Tool-call chips show "⚙ <name>: <preview>" via a small `formatAgentToolPreview` helper that knows about all four AGENT_TOOLS shapes (forge_asset / list_assets / set_project_memory / apply_recipe). No-key state surfaces an inline error chip rather than 401ing. Quirk: had to declare the scrollRef prop as `RefObject<HTMLDivElement>` (not `HTMLDivElement | null`) to match the existing useRef typing. Build clean. Next: Agent executes tools.

2026-05-07 fire #53 — Project share link — upload: installed `@vercel/blob` (^2.3.3) and added `app/api/share/route.ts` (Node runtime, `maxDuration = 30`) — accepts `multipart/form-data` with a single `file` field, 50 MB cap, calls `put("shared/<uuid>.zip", file, { access: "public", contentType: "application/zip" })` and returns `{url, id}`. 500s with a "connect a Vercel Blob store" hint when `BLOB_READ_WRITE_TOKEN` is missing so the failure mode is legible at deploy time rather than in the route handler. Refactored `exportProject` in `app/page.tsx` to delegate the heavy lifting to a new `buildProjectZipBlob()` helper that returns `{blob, slug} | null`; existing download path is now a 6-line wrapper around the helper. New `shareProject()` calls the same helper, builds a `FormData` with the zip blob, POSTs to `/api/share`, copies the returned `data.url` to clipboard via `navigator.clipboard.writeText`, and surfaces a 3 s "Link copied!" toast (new `shareToast` state, fixed-positioned bottom-center pill rendered alongside the existing modals). `ProjectSwitcher` gained an `onShare: () => void` prop and a 🔗 button next to the existing ⬇ Export button — same hover styling pattern as the surrounding buttons. No changes to import path or to any of the existing zip structure. Build clean (new dynamic route appears in the build output as `ƒ /api/share`). Next: Project share link — import.

2026-05-07 fire #54 — Project share link — import: extended `app/api/share/route.ts` with a GET handler that takes `?id=<id>`, calls `list({ prefix: "shared/<id>", limit: 1 })` from `@vercel/blob`, and 302-redirects to the matching public blob URL (404 if missing, 400 if id fails the `[a-zA-Z0-9-]+` regex, 500 if `BLOB_READ_WRITE_TOKEN` isn't configured — same hint as POST). On the client, added `importingShared` state in `app/page.tsx` and a one-shot `useEffect` (ref-guarded via `importedSharedRef`) that runs after `hydrated` becomes true: parses `window.location.search` for `?import=<id>`, fetches `/api/share?id=<id>` (browser auto-follows the 302), wraps the response blob in a `File` named `<id>.zip`, and pipes it through the existing `importProject(file)` flow. The `import` query param is stripped via `history.replaceState` in a `finally` block so a refresh after a successful import doesn't re-trigger; the toggle is also cleared on error paths. Header gained a small "Importing shared project…" spinner row next to the StorageIndicator (border-t-transparent + animate-spin). No changes to `importProject` itself or to existing share-upload flow. Build clean. Next: Real-time scene sync.

2026-05-07 fire #55 — Real-time scene sync: installed `@supabase/supabase-js` (^2.105.3) and added `app/lib/sceneSync.ts` exposing a memoised `getSceneSyncClient()` (reads `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`, returns null when either is missing), a `SYNC_PEER_ID` per-tab uuid for echo filtering, `openSceneChannel()` (broadcasts on event `scene` with `{peerId, scene}` payload, `self:false`, status callback emits joining/live/closed/error), and `broadcastScene()`. New `Project.syncEnabled?: boolean` (off by default, undefined when false to keep migration shape clean) plus `setSyncEnabled` mutator in `app/page.tsx`. `updateScene` gained an `opts.remote?: boolean` flag — when local (`!remote`) and the active scene matches, it fires a `broadcastScene(channel, next)` after applying. New refs `syncChannelRef` / `syncSceneIdRef` plus `syncStatus` state ("off"|"joining"|"live"|"error"); a useEffect keyed on `[currentId, activeSceneId, syncEnabled]` opens (or tears down) the channel and routes remote payloads back through `updateScene(remoteScene.id, () => remoteScene, {record:false, remote:true})` so they don't pollute the undo stack or echo. UI: ProjectStyleSection got two new props (`syncEnabled` + `onChangeSyncEnabled` + `syncStatus`) and a new section between the style refs and memory blob — `🌐 Real-time scene sync` checkbox with inline "● live" / "connecting…" / "✗ unavailable" status. ScenesView got a `syncStatus` prop and renders a green `● live` chip next to the rename/duplicate buttons in the scene header when the channel is up. `idbSet(PROJECTS_IDB_KEY, ...)` already serialises the whole project object so `syncEnabled` persists for free. Build clean (next reports no new dynamic routes — sync is browser-only). Next: ALL PHASES COMPLETE — only [SKIP-CRON] items remain.

2026-05-07 fire #52 — Agent executes tools: extended `AgentMsg` with a `tool_result` kind and added a `pendingAgentForges` queue + a useEffect that pops one entry at a time when the FORGE form is idle (sets genMode/quality, then triggers handleSubmit via the existing `pendingSubmitPrompt` replay path so the no-key Settings popup still works). New `executeAgentToolCalls(calls)` helper dispatches each tool: `forge_asset` queues onto `pendingAgentForges`, `list_assets` reads `currentProject.assets` (filters trashed, slim-maps to `{id,name,assetType,prompt}`, caps at 100, JSON-stringifies), `set_project_memory` calls the existing mutator, `apply_recipe` looks up by id then case-insensitive name and calls `applyRecipe(id)` — each returns a short `result` text the agent reads back. New `streamAgentTurn(history)` extracts the previous inline streaming logic from `sendAgentMessage` and adds tail-recursion: when the turn emits any tool calls, it executes them, appends `tool_result` chips to the visual log, and recurses with `[...history, {role:"assistant", content: turnAssistantText}, {role:"user", content: "Tool results:\n[name] result..."}]` — the route's 8-turn cap prevents runaway loops. `sendAgentMessage` now just builds the initial wire history (via new `buildWireFromLog(log)` helper that collapses contiguous `tool_result` chips into one synthesized user message per the roadmap's "next user message" design) and delegates to `streamAgentTurn`. AgentDrawer gained a green tool_result chip rendering (↳ name: result preview, truncated to 80 chars). Imports `ForgeAssetArgs`/`SetProjectMemoryArgs`/`ApplyRecipeArgs` types from `./lib/agentTools`. Files touched: `app/page.tsx` only. Build clean. Next: Project share link — upload.

2026-05-07 ALL PHASES COMPLETE — nothing to do. Every cron-eligible item across Phases 2–11 is checked; only `[SKIP-CRON]` items remain (page.tsx split, inpainting, CI/test suite, dropping the API-route proxy, multi-character party, scripted scenes, animation editor, concierge agent mode placeholder). No code changes this fire.

2026-05-07 ALL PHASES COMPLETE — nothing to do. Re-confirmed: zero unchecked roadmap entries across Phases 2–11; SKIP-CRON items unchanged. No code changes.

2026-05-07 ALL PHASES COMPLETE — nothing to do.

2026-05-07 fire #56 — Toast notification system: new `app/components/ToastHost.tsx` component with a module-level event bus — exports a `toast({ message, kind?, emoji? })` function that any component can call without prop-drilling. `ToastHost` subscribes via a listeners array, stacks toasts in a `fixed top-4 right-4 z-[60]` column, auto-dismisses after 3.5s, click-to-dismiss, 220ms fade-out via `opacity` transition. Three kinds (info / success / error) get different farm-palette borders + emoji defaults (ℹ️ / ✓ / ⚠). Migrated all 12 existing `alert()` sites in `app/page.tsx` to `toast({...})` — covers the share-import path (HTTP error + fetch error), `replaceSceneItem` failure, `shareProject` (reach-endpoint + share-failed), `importProject` (zip read, missing index, malformed index, success), sound-preview audio error, the failed-item retry stub, and the variety-check try-again stub. Pre-existing `shareToast` "Link copied!" pill kept as-is — separate flow with bottom-center placement. ToastHost mounted in the Home return alongside the other modals. `npx tsc --noEmit` clean (after `npm install`); `npx next build` clean. Next: Confirmation dialog component.

2026-05-07 fire #60 — Empty state illustrations + CTAs: replaced the three "nothing here yet" blocks across the right-panel tabs in `app/page.tsx` with elevated, padded placeholder cards that share a consistent farm-palette treatment — `border-2 border-dashed border-farm-wood/40 bg-farm-ink/30`, `max-w-xs` centered, `text-7xl` emoji, `font-pixel` headline + `text-xs opacity-70` sub-text. Asset gallery: forked the existing `recent.length === 0` branch — when `allAssets.length === 0` shows the elevated 🎨 card ("No assets yet — type a prompt to create one" + FORGE-input nudge); the search/filter no-match path keeps the simple opacity-60 line so it doesn't feel like the gallery is empty. Scenes empty state (🎬) and Recipes empty state (📋) reworded with the same shape — headline + one-line CTA pointing at the next user action. No new component file (3 inline blocks); no shared abstraction since the roadmap scoped this to "empty area, padded" and three sites is below the rule-of-three. `npx tsc --noEmit` and `npx next build` clean. Next: Loading skeletons during generation.

2026-05-07 fire #59 — Tooltip system: new `app/components/Tooltip.tsx` exports a `<Tooltip label=...>` wrapper that watches its inline-flex anchor span for pointer/focus events, opens after a 600 ms `setTimeout`, and renders a `position: fixed` farm-ink/farm-wood-bordered chip with `role="tooltip"` + `aria-describedby` linking the trigger to the tooltip node. `useLayoutEffect` reads the rendered tooltip's bounding box and clamps it: prefers above the anchor (`offset 6 px`), flips below when there's not enough room at the top, and clamps `left` to the viewport with an 8 px safety margin. Replaced `title` attributes on three groups in `app/page.tsx` (~13 sites total): four scene-controls "+ Trigger / + Light / + Emitter / + Sound" buttons in ScenesView, seven AssetCard action-row buttons (✏️ edit, 🎨 project style, 🎨🎯 palette, 🧵 seamless tile, 🪟 scene background, ⬇ PNG, 📦 frames+atlas zip), and the two header indicators (CostIndicator session/project breakdown + StorageIndicator usage/quota). Other `title=""` sites untouched per roadmap scope. Build clean. Next: Empty state illustrations + CTAs.

2026-05-07 fire #58 — Keyboard shortcuts help modal: new `app/lib/shortcuts.ts` exports a `SHORTCUTS: ShortcutGroup[]` constant — single source of truth grouping every cron-introduced shortcut into FORGE / Scene editor / Play mode / Global sections. Added `shortcutsOpen` state to `app/page.tsx` plus a tiny global-keydown effect that watches for `?` (Shift+/) outside of input/textarea/select/contentEditable targets and opens the modal. New `ShortcutsModal` component (defined next to `OnboardingModal` in page.tsx) renders a centered dark-overlay modal in the same farm-ink + `border-2 border-farm-wood` styling as the other modals, with each group rendered as a tinted-row table (`font-mono` keys column, description column). Esc dismisses, click-outside dismisses, ✕ button dismisses; `z-[65]` puts it above the other modals but below ConfirmHost (`z-[70]`). Footer surface: a "⌨ Shortcuts" button rendered just below the existing 🗑 Trash button in the gallery section, same border/hover styling pattern. Build clean. Next: Tooltip system.

2026-05-07 fire #61 — Loading skeletons during generation: added a `pwf-shimmer` keyframe + `.skeleton-shimmer` utility class to `app/globals.css` (animated linear-gradient sweeping across a `bg-farm-ink/60` box, gated behind `prefers-reduced-motion`). In the asset-gallery render in `app/page.tsx`, the empty-state branch now also gates on `!busy` so skeletons take over when a forge is in flight from a fresh project; the grid renders 1–4 skeleton placeholders at the top before the existing `recent.map(...)` — count is `Math.min(4, Math.max(1, variants))` so it tracks the form's variant slider. Each skeleton matches AssetCard's outer chrome (`bg-farm-ink/60 border-2 border-farm-wood p-2 flex flex-col gap-2`) with an aspect-square shimmer block + two short shimmer lines for the title rows; `aria-hidden="true"` so the placeholders don't pollute screen-reader output. Removed automatically when `busy` returns to false. Build clean. Next: Right-click context menu on assets.

2026-05-07 fire #62 — Right-click context menu on assets: new `app/components/ContextMenu.tsx` exports a `<ContextMenu x y items onClose>` component + `ContextMenuItem` type — fixed-position menu pinned to the cursor, `useLayoutEffect` clamps `left/top` so the menu never overflows the viewport (8 px safety padding), capture-phase `pointerdown` listener closes on outside click, Escape closes, each menu-item click also closes. Styled with the `bg-farm-ink + border-2 border-farm-wood` palette and `hover:bg-farm-grass/15` rows; destructive entries render in red. Added a tiny `duplicateAsset(id)` mutator in `app/page.tsx` next to `renameAsset` that clones the asset record with a fresh id, "(copy)" name suffix, `editedFrom` set, fresh `createdAt`, and `trashedAt` cleared. New `assetContextMenu` state (`{x,y,assetId} | null`) in Home; AssetCard gained an `onRequestContextMenu(x,y)` prop and an `onContextMenu` handler on the wrapper div that prevents the native menu and forwards the cursor coords — except inside `input/textarea` descendants so paste/spell-check still works in the rename / tag / edit fields. Menu items: ✏️ Edit (calls `startInlineEdit`), ⎘ Duplicate (calls `duplicateAsset` + success toast), 🎬 Add to scene (only present when `activeScene` is non-null — calls `addAssetToScene` at scene center then switches to the Scenes tab), 🏷 Tag… (`prompt()` pre-filled with existing tags, parses comma-separated list, calls `setAssetTags`), 🗑 Delete (calls `deleteAsset` — soft-delete via existing trash flow). Menu is rendered in the Home JSX root next to `ToastHost` / `ConfirmHost` so it overlays the entire app. `z-[75]` puts it above the regular modals (z-50/65) but below tooltips (z-80) and ConfirmHost (z-70 — that's intentional, so a destructive Delete inside the menu can still raise a confirm dialog later if we wire one in). Build clean. Next: Asset preview on hover.

2026-05-07 fire #57 — Confirmation dialog component: new `app/components/ConfirmDialog.tsx` mirrors the ToastHost event-bus pattern — module-level `confirm(opts)` returns `Promise<boolean>`, mounted singleton `<ConfirmHost />` subscribes via a listeners array, falls back to `window.confirm()` if no host is mounted (defensive — covers SSR/early-mount edge cases). `ConfirmHost` renders a centered modal in the same farm-ink + `border-2 border-farm-wood` styling as TrashModal/SettingsModal, dark `bg-black/70` overlay, click-outside dismisses, Escape resolves false, Enter resolves true, the confirm button is `autoFocus`'d. Supports `title`, `body` (multi-line via `whitespace-pre-line`), `confirmLabel`, `cancelLabel`, and a `destructive` flag (default true) that renders the confirm button red — `border-red-700 text-red-300` — vs farm-grass for non-destructive. Exports a `useConfirm()` hook that returns the same module-level function for ergonomic call-site one-liners. Migrated all five destructive `confirm()` flows named in the roadmap: `clearAll` (page.tsx ~3876), the trash-modal `onEmpty` callback (~4911), `ProjectSwitcher.handleDelete` (~4973), `RecipesView` per-row delete button (~5980), and `ScenesView` per-scene delete button (~6545). Each gained `async`/`await` + a more descriptive title + body pair. Three other `confirm()` sites (prefab delete, fill-layer, layer remove) were intentionally left alone since the roadmap scoped this item to the five destructive project/asset/scene/recipe flows. `<ConfirmHost />` mounted next to `<ToastHost />` in the Home JSX root. `z-[70]` keeps it above the other modals (Settings/Trash use `z-50`). Build clean. Next: Keyboard shortcuts help modal.

2026-05-07 fire #63 — Asset preview on hover: new `app/components/AssetHoverPreview.tsx` exports a `<AssetHoverPreview>` component that takes a `targetRef`, `src`, `alt`, `isTile`, and `suppressed` flag. Listens via `addEventListener` on `targetRef.current` for `mouseenter` / `mouseleave` / `dragstart` so it doesn't have to wrap the AssetCard's already-busy wrapper div with another element. Schedules a 400 ms reveal timer on enter; once the timer fires, captures `getBoundingClientRect()` and stores the anchor in state, then a `useLayoutEffect` computes a 2.5× sized fixed-position preview placed to the right of the card by default and flipped to the left when there's not enough viewport room — final `left/top` clamped to a viewport-safe rect with 8 px padding. `pointer-events: none` on the preview so it never intercepts hovers from the card under it. Three suppression paths: `suppressed` prop (turns off scheduling and retracts an open preview), `dragstart` event (closes immediately so the drag-image can take over), and a capture-phase `window.scroll` listener that closes whenever the gallery scrolls (otherwise the fixed-position preview would drift away from the moving card). Tile assets render the preview as a 3×3 repeating background — same pattern AssetCard already uses — so users can judge seamless tiling at zoom; non-tile assets render an `<img class="pixelated">` inside a `bg-checker` frame matching the card's chrome. `z-[78]` puts the preview above the context menu (z-75) and confirm dialog (z-70) but below tooltips (z-80) so existing UI never gets blocked. Wired into AssetCard in `app/page.tsx`: new `cardRef` + local `dragging` state at the top of the component, `ref={cardRef}` on the wrapper div, the existing inline `onDragStart`/`onDragEnd` handlers now also flip `dragging`, and `<AssetHoverPreview ... suppressed={selectMode || dragging || editing} />` rendered just before the card's closing `</div>` so the listeners attach inside the card's lifecycle. Build clean. Next: Animated transitions.

2026-05-07 fire #64 — Animated transitions: added three keyframes + utility classes to `app/globals.css` — `pwf-card-in` (200ms opacity 0→1 + translateY 4px→0, ease-out), `pwf-modal-in` (200ms opacity 0→1 + translateY -12px→0, ease-out), and `pwf-tab-fade` (150ms opacity 0→1, ease-out) — exposed as `.animate-card-in`, `.animate-modal-in`, `.animate-tab-fade`. All three are gated by a single `@media (prefers-reduced-motion: reduce) { animation: none }` block alongside the existing `.skeleton-shimmer` rule. AssetCard's wrapper div in `app/page.tsx` got the `animate-card-in` class so each card fades+rises on mount. The five in-file modals — PaletteModal (~5272), SettingsModal (~5465), TrashModal (~5699), OnboardingModal (~5790), ShortcutsModal (~5857) — and the failed-items modal at ~7937, plus `app/components/ConfirmDialog.tsx`'s inner panel, each got `animate-modal-in` on the inner content div. For the right-panel tab switch (assets/scenes/recipes), wrapped the existing conditional in a `<div key={rightTab} className="animate-tab-fade">` so React remounts the wrapper on each switch, restarting the 150ms cross-fade — no other tab logic touched. No new dependencies, no Tailwind config changes. Build clean. Next: Asset gallery density toggle.

2026-05-08 fire #65 — Asset gallery density toggle: new `galleryDensity` state ("comfortable" | "compact", default comfortable) in `app/page.tsx`, hydrated from `pixelplay:gallery-density` localStorage on mount and persisted on each toggle. Added a small "▤ / ▦" button in the gallery search/sort header, immediately right of the sort dropdown — flips state + writes to localStorage in one click; tooltip explains the next state. Asset gallery grid swaps from `grid-cols-2 lg:grid-cols-3 gap-3` (comfortable) to `grid-cols-2 lg:grid-cols-4 gap-2` (compact). AssetCard signature gained a required `density` prop; the wrapper div now picks `p-2 gap-2` vs `p-1.5 gap-1.5` and the title row tightens from `text-xs` to `text-[11px]` in compact — all other inner layout untouched per the roadmap's "padding/font-size only" scope. The forge-skeleton placeholder cards rendered while `busy` follow the same density-driven padding/gap so the gallery doesn't jump between widths when generations land. Files touched: `app/page.tsx` only. Build clean. Next: ALL PHASES COMPLETE — only [SKIP-CRON] items remain.

2026-05-08 ALL PHASES COMPLETE — nothing to do. Verified zero `- [ ]` entries across every phase of ROADMAP.md; only `[SKIP-CRON]` items remain (page.tsx split, inpainting, CI/test suite, dropping the API-route proxy, multi-character party, scripted scenes, animation editor, concierge agent mode placeholder). No code changes this fire.

2026-05-08 ALL PHASES COMPLETE — nothing to do.

2026-05-08 ALL PHASES COMPLETE — nothing to do.

2026-05-10 fire #73 — Diagonal walk handling: rewrote the input-to-facing logic in `app/components/ScenePlayer.tsx` (the rAF tick branch that sets `facingRef.current` after computing dx/dy). Old code used a strict `Math.abs(dx) > Math.abs(dy)` which on a perfect diagonal could flip back and forth between adjacent ticks as floating-point drift made one side win. New logic: compute `ax = |dx|`, `ay = |dy|`, then `useX = ax > ay ? true : ay > ax ? false : prev === "east" || prev === "west"` — strict-larger-axis wins; on an exact tie, keep the previous axis (sticky). Within the chosen axis, the sub-direction is the sign of dx/dy (both non-zero in the tie case). Net effect: a player holding Up+Right walks with a stable facing instead of stuttering between north and east; the walk-cycle no longer freezes from rapid facing flips. Single-axis input is unchanged. Build clean. Next: Style reference threaded through every split-items call.

2026-05-10 fire #72 — Walk-cycle slicing robustness: extended `sliceSheet` in `app/lib/sprites.ts` with a non-empty-cell detection pass before slicing. New `detectActualLayout(img, cols, rows)` helper draws the image to an offscreen canvas, samples the centre 50% of each requested cell via `getImageData`, and counts which rows have any pixel with alpha > 16. When only the first row is filled in a multi-row request (the gpt-image-1 failure mode where rows 2-4 of a 4×4 sheet come back blank), the helper returns `{cols, rows: 1}`. `sliceSheet` then slices the smaller actual shape and TILES the result into the originally-requested cols×rows so callers indexing by row/col still receive an array of the expected length — every row maps to row 0 modulo, so a sprite expected to walk in 4 directions falls back gracefully to "same walk-cycle in every direction" instead of garbage frames. All other partial-fill patterns and any canvas-read failure (CORS, undersized cells) fall back to the requested shape unchanged. No call-site changes needed because the output shape is preserved. Build clean. Next: Diagonal walk handling.

2026-05-10 fire #71 — Player + NPC shadow ellipses: characters now render a soft dark ellipse beneath them in Play mode, anchoring them visually to the ground. In `app/components/ScenePlayer.tsx`, inside the regular character/item wrapper div (just before the `<img>`), a sibling `<div>` is conditionally rendered when `isPlayer || npcState !== undefined || a.assetType === "character" || a.assetType === "creature"`. The shadow is `width: 60%`, `height: 14%`, positioned at `bottom: 0` with `transform: translate(-50%, 50%)` so half overlaps the feet line and half extends below — `borderRadius: 50%` plus a `radial-gradient(ellipse, rgba(0,0,0,0.28), rgba(0,0,0,0) 70%)` background gives the soft fade-out without a CSS filter. To unblock the conditional check, extended `CanvasAsset` in `app/components/SceneCanvas.tsx` with an optional `assetType?: string` field (kept loose to avoid pulling page.tsx's `AssetType` enum into the component module) and wired it through the `canvasAssets` builder in `app/page.tsx`. Pointer-events are off so the shadow never intercepts clicks. Files: `app/components/ScenePlayer.tsx`, `app/components/SceneCanvas.tsx`, `app/page.tsx`. Build clean. Next: Walk-cycle slicing robustness.

2026-05-10 fire #70 — Idle bobble: added a subtle sine-wave bob to the player sprite's rendered Y when stationary in `app/components/ScenePlayer.tsx`. New `lastMovedAtRef = useRef(0)` next to `movingRef`/`frameIdxRef`; updated to `performance.now()` whenever the rAF tick applies non-zero movement (the same `dx !== 0 || dy !== 0` branch that already sets `movingRef.current = true`). At render time, when the rendered item is the player AND `performance.now() - lastMovedAtRef.current > 250`, an `idleBobble = Math.sin(now * Math.PI / 600)` offset is added to `renderY` (period 1200 ms, amplitude 1 scene unit). NPCs and static items unchanged. Bobble stops the moment movement input arrives because `lastMovedAtRef` updates every moving frame, naturally pushing the elapsed back under the 250 ms threshold. The component already re-renders each rAF tick via the existing `forceTick` / `pickedTick` triggers, so the sine wave animates smoothly without any new render scheduling. Build clean. Next: Player + NPC shadow ellipses.

2026-05-10 fire #69 — Scale enforcement in scene-layout: extended the `gptLayout` system prompt in `app/api/scene-layout/route.ts` with an explicit SCALE RUBRIC block — four item categories with strict ranges: trees / large buildings / towers / statues / ships 0.25–0.40, characters / NPCs / creatures 0.15–0.20, mid-size props (furniture, vehicles, signs, fountains, anvils, large plants) 0.10–0.18, small ground props (rocks, mushrooms, flowers, coins, food, bones, seashells) 0.06–0.10. Added a worked "forest with a cabin" example (cabin 0.35, pine tree 0.30, oak 0.28, mossy rock 0.10, mushroom 0.07) so the model has a concrete reference for relative sizing. Tightened the defensive clamp on parsed scale from `clamp(.., 0.05, 0.6)` to `clamp(.., 0.04, 0.5)` so even when the LLM ignores the rubric no single item can dominate the canvas or shrink off-screen. Updated COMPOSITION RULES to cross-reference the rubric (focal item at TOP of its range, supporting items at bottom-to-middle). Build clean. Next: Idle bobble.

2026-05-10 fire #68 — Y-sort player into the painter list: replaced z-index-based stacking in `app/components/ScenePlayer.tsx` with a y-sort painter algorithm. New `depthOf(it)` helper returns the item's bottom-edge y in scene coordinates: player + moving NPCs use their runtime y from `posRef.current` / `npcStateRef`, static items use their authored `it.y`; "bottom"-anchored items take y as-is (already at the foot), "center"-anchored items add `it.scale * longest * 0.5` as an approximate half-sprite-height. The `sortedItems` comparator now sorts by `depthOf` ascending instead of `it.z`, and the regular character/item render branch sets `zIndex: Math.round(depthOf(it))` instead of `zIndex: it.z`. Light/emitter/trigger overlays keep their existing `it.z` zIndex so they continue to sit above gameplay sprites. Net effect: a tree at y=400 draws over a tree at y=300 (no longer arbitrary by z), the player correctly walks BEHIND a tree as they approach it from the south and IN FRONT as they walk past, and patrol NPCs re-stack with the world as they move along their waypoints. The four other call sites of `sortedItems` (mini-map, dialogue bubbles, use-hint, click-to-walk hit test) are filter/iterate operations that don't depend on sort order. Build clean. Next: Scale enforcement in scene-layout.

2026-05-10 fire #67 — Auto-solid for blocker items: new `defaultSolidForName(name)` helper in `app/page.tsx` (next to the existing `defaultSolid(assetType)`) does a whole-word regex match against a `BLOCKER_KEYWORDS` list of ~50 phrases — architecture (cabin, house, cottage, building, tower, shop, tavern, barn, windmill, lighthouse), monuments (statue, fountain, obelisk, pillar, gravestone, tombstone, altar, shrine), trees (tree, pine, oak, fir, spruce, birch, willow, palm, bush, shrub), rocks (boulder, rock, stone wall), and heavy interior furniture (anvil, workbench, forge, cauldron, fireplace, dresser, wardrobe, bookshelf, bed, sofa, piano, throne, desk, table). Wired into both new-item placement paths: `composeSceneFromAssets` (split-items) and `addAssetToScene` (drag-from-gallery) now compute solid as `defaultSolid(asset.assetType) || defaultSolidForName(asset.prompt) || defaultSolidForName(asset.name)`. So a generated "tree" with assetType="item" still becomes a player obstacle, matching the player's intuition that they should walk AROUND visible obstacles. Doesn't override explicit user toggles (the existing `toggleSceneItemSolid` flips the value cleanly either way). Build clean. Next: Y-sort player into the painter list.

2026-05-09 fire #66 — Auto-background tile per scene: added `autoBackgroundColor?: string` to the `Scene` type in `app/page.tsx` and the matching `CanvasScene` shape in `app/components/SceneCanvas.tsx` (so `PlayerScene = CanvasScene & {...}` picks it up too). New `autoBackgroundColorForContext(context)` helper next to `ensureGroundLayer` returns `#c9a779` for interior (warm light wood-plank), `#7cb86b` for exterior (matches the existing `farm-grass` token), `#d6c08a` for aerial (sand-tan), and `undefined` otherwise. `composeSceneFromAssets` now sets `autoBackgroundColor: autoBackgroundColorForContext(context)` on the new scene record alongside `failedItems` — so the field is auto-derived at compose time from the parsed `extractScene` context. In `ScenePlayer.tsx`, the inner-world div's inline style spreads `{ backgroundColor: scene.autoBackgroundColor }` when defined, sitting under the existing tile-grid layers and item z-stack — so a scene with no painted tile cells (or a partially-erased grid) shows a context-appropriate ground color instead of the dark editor checker. The existing `bg-checker` class still provides the subtle alpha texture pattern on top. No new asset generation, no migration concerns (legacy scenes stay `undefined` and fall back to checker as before). Files touched: `app/page.tsx`, `app/components/SceneCanvas.tsx`, `app/components/ScenePlayer.tsx`. Build clean. Next: Auto-solid for blocker items.
