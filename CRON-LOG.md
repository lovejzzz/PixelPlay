# Cron log

One line per fire — what got done and what's next. Newest at the bottom.

## 2026-05-05 — initial setup

Roadmap (`ROADMAP.md`) seeded with Phase 2-5 items (~26 entries). Cron will
fire every 5 minutes and tick off 3 unchecked items per fire.

2026-05-05 fire #1 — flipX + flipY toggles (SceneItem field, render in SceneCanvas/ScenePlayer, side-panel + hierarchy buttons, persisted in scene + project export manifests, baked into composite PNG via new drawWithFlip helper) and numeric x/y/scale inputs in side panel (kept slider, added editable percent input + x/y number boxes). Build clean. Next 3: pan camera with middle-click drag, shift-click multi-select, drag-rectangle multi-select.

2026-05-05 fire #2 — middle-click pan (walks ancestry to find scrollable parent, translates scrollLeft/scrollTop while held), selection model upgraded `string | null` → `string[]` with auto-migration from old IDB persisted state, shift-click toggles membership, rubber-band rectangle on primary pointerdown over empty canvas with green overlay and on-release hits-by-center. Side panel shows multi-select summary when >1 selected. Build clean. Next 3: group move with multi-select, undo/redo (Cmd+Z / Cmd+Shift+Z), rotation handle.

2026-05-05 fire #3 — group move (drag captures groupStart for non-anchor selected items, onMoveItems batches absolute positions; new moveSceneItems mutator), undo/redo (per-scene past/future stacks capped at 30 in in-memory sceneHistory state, updateScene records before mutating with optional `record:false` for play-mode position writebacks, Cmd+Z / Cmd+Shift+Z handlers in scene keydown effect), rotation (rotation field on CanvasItem + SceneItem, sky-blue rotation handle 28px above selected item with connector tick, drag computes scene-coord angle from item center, rotate gesture in SceneCanvas, transform composes translate+rotate, ScenePlayer applies rotation to non-player items, drawWithFlip helper extended to rotate then flip, manifest exports include rotation). Build clean. Note that fire was a continuation — group move + undo/redo were carried over from an interrupted prior run. Next 3: snap-to-other-items alignment guides, pickable flag on SceneItem, pickup behavior in play mode.

2026-05-05 fire #4 — snap-to-other-items guides (during item drag, finds nearest other item center within 6 scene px on each axis and snaps to it; renders thin green crosshair lines while active, cleared on pointerup), pickable flag (field on SceneItem + CanvasItem, side-panel 🛒 Pickup button, hierarchy 🛒 toggle, persisted in scene + project manifests; rotation also added to scene manifest which had been missing), pickup behavior in play mode (PICKUP_RADIUS=16 proximity check each rAF tick using pickedIdsRef so picked items vanish in O(1), toast queue with 1500ms TTL rendered along bottom of viewport, picked items filtered from sortedItems render). Build clean. Next 3: inventory HUD, door/portal items, NPC patrol path field.

2026-05-05 fire #5 — inventory HUD (top-right corner overlay during play mode listing picked-item thumbnails, scrolls/wraps), door/portal flag (linkSceneId on SceneItem + CanvasItem with a side-panel "🚪 Door" select listing other project scenes; ScenePlayer detects player-vs-portal proximity at PICKUP_RADIUS, fires onPortalEnter once on entry — re-arms when player leaves all portal AABBs via insidePortalRef; page handler switches activeSceneId and re-picks first character in target scene), NPC patrol (patrol field on SceneItem + CanvasItem with points/loop/speed; side-panel section visible for character/creature items with "+ Add waypoint at current pos", per-waypoint remove, loop checkbox, speed input; SVG overlay on selected items renders dashed connecting lines + numbered cyan dots; persisted in scene + project manifests). Build clean. Next 3: NPC patrol play behavior, trigger zone item type, tile painting tool.

2026-05-05 fire #6 — NPC patrol play behavior (npcStateRef holds per-NPC runtime: x/y/idx/forward/facing/moving/frameIdx; rAF step loop walks each NPC toward current waypoint at patrol.speed px/s, advances index on arrival, supports loop and ping-pong; per-NPC sprite-sheet caching via npcFramesRef so 4×4 full-sheet NPCs animate directionally and 1×4 walk-cycles mirror west-facing; render fork uses runtime position + appropriate frame). Trigger zone item type (kind?: "trigger" + triggerMessage on SceneItem; "+ Trigger" button in scene controls injects a 18%-scale zone at scene center; SceneCanvas renders trigger items as sky-blue translucent square with ⚡ icon and label, fully draggable via existing item gesture; new side-panel branch for trigger items shows message input + numeric x/y/size; ScenePlayer skips trigger render in play mode, AABB-tests player against each trigger half-extents, fires triggerMessage into a 5-entry log overlay bottom-right with re-arm-on-exit; persisted in scene + project manifests). BLOCKED: tile painting tool — schema extension + paint-mode state + tile picker + render-everywhere is a multi-fire feature, deferred to next fire as a focused attempt. Build clean. Next 3 (next fire): tile painting tool, eraser for tile layers, multiple tile layers.

2026-05-05 fire #7 — full tile-painting cluster landed in one shot. Schema: `Scene.tileGrid?: { tileSize; layers: TileLayer[] }`; TileLayer has id/name/tileAssetId/cells/visible. Mutators in page: addTileLayer, removeTileLayer, renameTileLayer, setLayerTileAsset, toggleLayerVisible, reorderTileLayers, paintTileCell, eraseTileCell, setTileSize. Page state: paintMode ("off" | "paint" | "erase") and activeTileLayerId. SceneCanvas: new "paint" drag-kind triggers from primary-button on empty canvas when paintMode is on; floor-divides scene coords by tileSize to find target cell; held drag keeps stamping/erasing each frame. Tile cells render below items as absolute-positioned `<img>` tiles (z-index -1); overlay grid pattern shows during paint mode. Multi-layer side panel (TilePaintBar): + Layer button, per-layer row with drag-handle for z-order, visibility toggle (👁), inline rename, tile-asset dropdown filtered to assetType==="tile", cell count, delete with confirm. Tile painting renders identically in ScenePlayer. Composite PNG export bakes tile cells before items in both per-scene and full-project zips. Manifest fields include tileGrid in both. Build clean. Next 3: prefab system, linked instances, point-light item type.

2026-05-05 fire #8 — Prefab system: Project.prefabs Record + Prefab type with master-items array; "💾 Save as prefab" button in the multi-select hint prompts for a name and clones selected items as masters. PrefabLibrary panel below the hierarchy lists prefabs with item-count, sync, and delete; rows are draggable with application/x-pwf-prefab-id mime type, dropping onto SceneCanvas calls instantiatePrefab which creates fresh items at the drop point with bbox-centered offset and prefabId/prefabSourceId tags. Linked instances: prefabId + prefabSourceId on SceneItem; syncPrefabInstances iterates all scenes and pulls non-position fields (asset, scale, flip, rotation, etc.) from the master into every instance, preserving x/y/z/id; exposed via 🔄 button in the prefab library. Point-light item type: kind "light" + light: { radius, color, intensity }; "💡 + Light" button in scene controls injects a light at scene center with warm 200px default; SceneCanvas edit-mode renders a 💡-circle with box-shadow; ScenePlayer renders a radial-gradient overlay sized to 2*radius with mix-blend-mode: screen and alpha from intensity; new side-panel branch with color picker, radius/intensity sliders, x/y inputs. Manifests carry prefabId/prefabSourceId/light. Build clean. Next 3: particle emitter item, day/night tint slider, sound-trigger placeholder.

## 2026-05-05 — ROADMAP COMPLETE

2026-05-05 fire #9 — last 3 items: particle emitter (kind "emitter" + emitter: {kind:"sparkle"|"smoke", rate, lifetime}; ✨ + Emitter button; SceneCanvas edit-mode shows dashed ✨ placeholder; ScenePlayer renders 2-10 deterministic-randomly-positioned dots with new pwf-sparkle / pwf-smoke CSS keyframes for each emitter at staggered animation delays; side-panel kind toggle + rate/lifetime sliders), day/night tint (Scene.daytime ∈ [0,1] with new ☀️ slider in scene controls bar; ScenePlayer overlays an absolute mix-blend-multiply div with daytimeTint() lerping a 5-stop palette midnight→dawn→noon→dusk→midnight; setSceneDaytime flagged record:false so dragging the slider doesn't blow out the undo stack), sound-trigger (kind "sound" + sound: {url, volume, loop}; 🔊 + Sound button; SceneCanvas edit-mode shows dashed 🔊 placeholder, hidden in play; rAF detects bbox entry like other triggers, instantiates Audio() with volume+loop, plays on entry, pauses looping audio on exit; insideSoundRef + audiosRef per-id; cleanup on unmount and on player change). All new fields persisted in scene + project manifests. Build clean. ROADMAP COMPLETE — 26/26 items done. Cron should now exit early on subsequent fires.

2026-05-05 fire #10 — ROADMAP COMPLETE — nothing to do.

2026-05-05 fire #11 — ROADMAP COMPLETE — nothing to do.

2026-05-05 fire #12 — ROADMAP COMPLETE — nothing to do.

## 2026-05-06 — Phase 6 begins (polish & robustness)

User asked for a deep audit after dogfooding. New roadmap added as
Phase 6 — 17 items in priority order, plus 7 `[SKIP-CRON]` items
deferred to manual work. Cron pace: 1 item per fire, hourly at :13
(7-day auto-expiry caps total runs at 168, well above what's needed).
Items concentrate on UX polish (camera follow, fill tools, soft-delete,
duplicate scene, ⌘+Enter, settings test-connection) rather than
deep features.
